global:
  scrape_interval: 30s
  evaluation_interval: 10s

rule_files:
  - /etc/prometheus/alert_rules.yml

scrape_configs:
  - job_name: 'flask-app'
    static_configs:
      - targets: ['flask:5000']
